- [x] Create new directory structure (`packages/`, `services/`, `apps/`)
- [x] Create core config (`packages/core/config.py`)
- [x] Create utilities (`packages/utils/utils.py`, `packages/utils/visualize.py`, `packages/utils/cleaner.py`)
- [x] Create camera module (`packages/camera/camera.py`)
- [x] Create AI tasks (`packages/ai/tasks/anomaly.py`, `packages/ai/tasks/yolo_detector.py`)
- [x] Create AI engineering tools (`packages/ai/engineering/data_collector.py`)
- [x] Create database services (`services/database/models.py`, `services/database/session.py`, `services/database/crud.py`)
- [x] Create workflow orchestration (`packages/workflow/pipeline.py`)
- [x] Create web app endpoint (`apps/edge-agent/web_backend.py`)
- [x] Update `run_web_backend.bat` to point to the new `apps/edge-agent/web_backend.py` với cấu hình `PYTHONPATH`.

**Giai đoạn 2 (Hoàn tất):**
- [x] Tạo thư mục `scripts/` và tạo lại các script chạy (.bat) bên trong.
- [x] Tạo `pyproject.toml` cho Root workspace.
- [x] Tạo `pyproject.toml` cho `packages/core`.
- [x] Tạo `pyproject.toml` cho `packages/utils`.
- [x] Tạo `pyproject.toml` cho `packages/camera`.
- [x] Tạo `pyproject.toml` cho `packages/ai`.
- [x] Tạo `pyproject.toml` cho `packages/workflow`.
- [x] Tạo `pyproject.toml` cho `services`.
- [x] Tạo `pyproject.toml` cho `apps/edge-agent`.

**Giai đoạn 3 (The Complete Skeleton - Hoàn tất):**
- [x] Tạo cấu trúc thư mục rỗng cho `packages/ai/tasks/`
- [x] Tạo cấu trúc thư mục rỗng cho `packages/ai/engines/`
- [x] Tạo cấu trúc thư mục rỗng cho `packages/ai/engineering/`
- [x] Tạo cấu trúc thư mục rỗng cho `packages/ai/pipeline/`
- [x] Tạo cấu trúc thư mục rỗng cho `services/` (camera, inference, workflow, deployment, notification)
- [x] Tạo cấu trúc thư mục rỗng cho `apps/` (dashboard, annotation, training, cli)
- [x] Tạo cấu trúc thư mục rỗng cho `solutions/` (smart-factory, warehouse, retail, traffic, robotics)
- [x] Tạo cấu trúc thư mục rỗng cho `infrastructure/`
