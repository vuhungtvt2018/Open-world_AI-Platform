[project]
name = "services"
version = "0.1.0"
description = "Database and internal microservices"
requires-python = ">=3.10"
dependencies = [
    "core",
    "utils",
    "sqlalchemy",
    "numpy"
]

[tool.uv.sources]
core = { workspace = true }
utils = { workspace = true }
