[project]
name = "workflow"
version = "0.1.0"
description = "Orchestrates AI and Camera tasks"
requires-python = ">=3.10"
dependencies = [
    "core",
    "utils",
    "camera",
    "ai",
    "services",
    "requests",
    "opencv-python",
    "numpy"
]

[tool.uv.sources]
core = { workspace = true }
utils = { workspace = true }
camera = { workspace = true }
ai = { workspace = true }
services = { workspace = true }
