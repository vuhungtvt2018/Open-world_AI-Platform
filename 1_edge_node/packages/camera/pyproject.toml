[project]
name = "camera"
version = "0.1.0"
description = "Camera handling for vision AI platform"
requires-python = ">=3.10"
dependencies = [
    "opencv-python",
    "core"
]

[tool.uv.sources]
core = { workspace = true }
